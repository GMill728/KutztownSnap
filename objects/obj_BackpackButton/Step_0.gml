
//If the esc menu is pulled up then it cant be seen, also set a variable to turn it all off
if (obj_EscMenu.EscMenuPulledUp = true){
	visible = false;
} else if (obj_EscMenu.makeInvisible = true){
	visible = false;
} else {
	visible = true;
}